export class carDetails {
    carId: number;
    carName: string;
    model: string;
    seats: string;
    fuelCapacity: string;
    airBags: string;
    fuelType:string;
    carType: string;
    registrationNumber: string;
    transmission: string;
    kmsDriven: string;
    price: string;
    city:string;
    ownerMailId: string;
    dataPresence: string;
    // --16
    // carID, carName, model, seats, fuelCapacity, airbags, fuelType, carType, transmission, 
    // registrationNumber, kmsDriven, price, rating, ownerMailId, city, dataPresence
    // carName: string,
    // model: string,
    // seats: string,
    // fuelCapacity: string,
    // airbags: string,
    // fuelType:string,
    // carType: string,
    // registrationNumber: string,
    // transmission: string,
    // kmsDriven: string,
    // price: string,
    // city:string
}